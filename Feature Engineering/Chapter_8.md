## 🧭 فصل ۲: انواع داده و آماده‌سازی اولیه

### 📄 صفحه ۳ از ۵

✍️ *نویسنده: سیامک عباس‌نژاد*
🌐 *[https://github.com/pydevcasts](https://github.com/pydevcasts)*

---

### 🔤 چرا باید داده‌های متنی را عددی کنیم؟

اغلب مدل‌های یادگیری ماشین (مثل رگرسیون، درخت تصمیم، یا شبکه عصبی)
فقط داده عددی رو می‌فهمن، نه رشته‌ای (String).

📌 یعنی باید ستون‌هایی مثل «جنسیت»، «شهر»، یا «نوع محصول»
رو به شکل عددی دربیاریم تا مدل بتونه بینشون رابطه پیدا کنه.

---

### ⚙️ روش‌های عددی‌سازی داده‌های متنی (Encoding Methods)

سه روش اصلی وجود داره:

| روش                                | توضیح کوتاه                              | مناسب برای                |
| :--------------------------------- | :--------------------------------------- | :------------------------ |
| 🔢 **Label Encoding**              | تبدیل هر دسته به یک عدد خاص              | داده‌های ترتیبی (Ordinal) |
| 🧩 **One-Hot Encoding**            | ساختن ستون مجزا برای هر دسته             | داده‌های اسمی (Nominal)   |
| 🧠 **Binary / Frequency Encoding** | جایگزینی دسته با مقدار باینری یا فراوانی | داده‌های زیاد و تکراری    |

---

### 💡 ۱.  برچسب‌گذاری عددی (Label Encoding)

برای داده‌های دارای ترتیب مثل سطح تحصیلات یا سایز لباس.

```python
from sklearn.preprocessing import LabelEncoder
import pandas as pd

data = pd.DataFrame({'education': ['Diploma', 'Bachelor', 'Master', 'PhD']})

encoder = LabelEncoder()
data['education_encoded'] = encoder.fit_transform(data['education'])
print(data)
```

📊 خروجی:

| education | education_encoded |
| :-------- | :---------------- |
| Diploma   | 0                 |
| Bachelor  | 1                 |
| Master    | 2                 |
| PhD       | 3                 |

📌 ترتیب در اینجا مهمه چون بین «دیپلم» تا «دکتری» سطح افزایش پیدا می‌کنه.

---

### 💡 ۲. رمزگذاری تک‌گرمی (One-Hot Encoding)

برای داده‌های بدون ترتیب مثل شهر، کشور، یا جنسیت.

```python
data = pd.DataFrame({'city': ['Tehran', 'Isfahan', 'Shiraz', 'Tehran']})
encoded = pd.get_dummies(data, columns=['city']).astype(int)
print(encoded)
```

📊 خروجی:

| Tehran | Isfahan | Shiraz |
| :----: | :-----: | :----: |
|    1   |    0    |    0   |
|    0   |    1    |    0   |
|    0   |    0    |    1   |
|    1   |    0    |    0   |

📌 هر شهر تبدیل به یک ستون جدا شده و ۱ یعنی وجود اون دسته در ردیف مربوطه.

---

### 💡 ۳.  رمزگذاری بر اساس فراوانی (Frequency Encoding)

وقتی تعداد دسته‌ها زیاد باشه (مثلاً صدها شهر مختلف)، One-Hot Encoding باعث زیاد شدن ستون‌ها می‌شه.
در این حالت از **فراوانی تکرار هر دسته** استفاده می‌کنیم:

```python
data = pd.DataFrame({'city': ['Tehran', 'Isfahan', 'Shiraz', 'Tehran', 'Tehran']})

freq = data['city'].value_counts()
data['city_encoded'] = data['city'].map(freq)
print(data)
```

📊 خروجی:

| city    | city_encoded |
| :------ | :----------- |
| Tehran  | 3            |
| Isfahan | 1            |
| Shiraz  | 1            |
| Tehran  | 3            |
| Tehran  | 3            |

📌 حالا شهرهایی که بیشتر تکرار شدن، مقدار عددی بزرگ‌تری گرفتن.

---

### 🧠 نکته‌ی طلایی:

> انتخاب نوع Encoding کاملاً بستگی به نوع داده داره:
> اگر داده‌ت **ترتیب‌دار** بود → Label Encoding
> اگر **بدون ترتیب** بود → One-Hot Encoding
> اگر **تعداد دسته‌ها زیاد** بود → Frequency Encoding

---

### 🎨 تصویر پیشنهادی

> یک تصویر با سه بخش:
> ۱️⃣ ستون متنی اولیه
> ۲️⃣ سه روش عددی‌سازی مختلف (Label, One-Hot, Frequency)
> همراه با فلش‌هایی که نشان می‌دهند هر روش چطور داده را تبدیل می‌کند 🔁📊

---

### 📚 مثال ترکیبی واقعی

فرض کن دیتاست زیر را داری:

| نام  | جنسیت | شهر    | سطح تحصیلات   |
| :--- | :---- | :----- | :------------ |
| علی  | مرد   | تهران  | کارشناسی      |
| سارا | زن    | شیراز  | کارشناسی ارشد |
| رضا  | مرد   | اصفهان | دکتری         |

بعد از عددی‌سازی می‌تونه اینطوری بشه:

| gender_male | gender_female | city_Tehran | city_Shiraz | city_Isfahan | edu_level |
| :---------: | :-----------: | :---------: | :---------: | :----------: | :-------: |
|      1      |       0       |      1      |      0      |       0      |     1     |
|      0      |       1       |      0      |      1      |       0      |     2     |
|      1      |       0       |      0      |      0      |       1      |     3     |

---

### 🧠 تمرین کوچک

داده‌ی زیر را در نظر بگیر 👇

|  رنگ | تعداد خرید |
| :--: | :--------: |
| قرمز |     10     |
|  آبی |      5     |
| قرمز |      7     |
|  سبز |      8     |

سعی کن رنگ‌ها را با روش Frequency Encoding به عدد تبدیل کنی ✏️

---

### ❓ پرسش چهارگزینه‌ای

در کدام حالت بهتر است از One-Hot Encoding استفاده کنیم؟

A) سطح تحصیلات (دیپلم، کارشناسی، ارشد)

B) رنگ ماشین (قرمز، آبی، سبز) ✅

C) رتبه مشتری (Bronze, Silver, Gold)

D) اندازه لباس (S, M, L, XL)

